const API_URL = 'https://frontend-exam.digitalfortress.dev';

export const API_ENDPOINTS = {
  LOGIN: `${API_URL}/auth/login`,
  PROJECTS: `${API_URL}/projects`,
  CHECK_TOKEN: `${API_URL}/auth/refresh-token`,
};
